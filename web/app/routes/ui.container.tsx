import React from 'react';
import {Container} from '~/components/container';

export default function ContainerUi() {
	return (
		<Container>
			<h1>Container</h1>
			<p>
        Containers are used to wrap content and apply padding and margin to the content.
			</p>
		</Container>
	);
}
